"""Temporary legacy contour retained to preserve current PhotoHome behaviour.

The modules ``main``, ``storage``, ``library``, ``library_api``, ``analyzer``,
``uploads`` and ``album_service`` remain in their current locations until later
migration tasks introduce their application commands and infrastructure adapters.
"""
